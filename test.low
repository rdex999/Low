float[10] arr;
float offset = 13.12321;
for int i=0; i<10; ++i{
    arr[i] = (offset + 4.2)*23.7;
    offset++;
}

for int i=0; i<10; ++i{
    printFloat32(arr[i] , 10);
    printChar(10);
}

printChar(10);
printFloat32(*(arr + 8), 10);
printChar(10);